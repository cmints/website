<% for (let navigation of site.navigations) { %>
  <li>
    <a href="/<%= navigation.path %>"
      <% if (page.pathname == navigation.path || page.pathname.includes(navigation.contains)) { %>class="active"<% } %>>
      {<%-navigation.stringId%>(navigation)}
    </a>
  </li>
<% } %>