---
layout: home
title: CMS with the internationalization done right
description: CMintS is a CMS created with the internationalization in mind. Comprehensive i18n implementation, create themes using EJS templating engine and LESS, write content using Markdown and get TMS integration out of the box.
---
<section id="main">
  <div class="content device">
    <a href="/" class="cell1 end">
      <img id="logo" src="/images/logo.png">
    </a>
    <h2>{i18n-in-mind[Main text] CMS created with the internationalization in mind}</h2>
    <p>
      <a <%-href("documentation")%> class="button">{get-started[Main button text] Get started}</a>
    </p>
  </div>
</section>
<section id="features">
  <div class="content">
    <h1 class="project-name">{features[Features header] Features}</h1>
  </div>
  <div class="col4 cards device">
    <div>
      <h2>{i18n[Feature header] i18n}</h2>
      <p>{i18n-msg["i18n" feature text] Comprehensive internationalization out of the box.}</p>
      <a <%-href("documentation/i18n")%> class="button">{learn-more[Feature button text] Learn more}</a>
    </div>
    <div>
      <h2>{theme[Feature header] Theme}</h2>
      <p>{theme-msg["Theme" feature text] Start creating themes using EJS templating engine and LESS.}</p>
      <a <%-href("documentation/themes")%> class="button">{learn-more}</a>
    </div>
    <div>
      <h2>{markdown[Feature header] Markdown}</h2>
      <p>{markdown-msg["Markdown" feature text] Get CommonMark out of the box to create content faster.}</p>
      <a <%-href("documentation/pages")%> class="button">{learn-more}</a>
    </div>
    <div>
      <h2>{tms[Feature header] TMS integration}</h2>
      <p>{tms-msg["TMS integration" feature text] Handy API to integrate your project with the Crowdin.}</p>
      <a <%-href("documentation/i18n/crowdin")%> class="button">{learn-more}</a>
    </div>
  </div>
</section>
